<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <meta name="title" content="{{ doc.get('title') }}">
    <meta name="description" content="{{ doc.get('title') }}">
    <meta name="author" content="Bug Business">

    <title>{{ doc.get('title') }}</title>
</head>
<body>

    <div>
        The title of this document is: {{ doc.get('title') }}
    </div>

</body>
</html>